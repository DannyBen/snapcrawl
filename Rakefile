# This file is here only because people and travis are used to
# running 'rake'. You can run 'run test' for the same output.

task :default do
  Dir['./test/test_*.rb'].each { |file| require file }
end